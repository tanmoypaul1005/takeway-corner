"use client";
import React from 'react';
import CommonButton from '../components/button/CommonButton';

const  AppointmentButton :  React.FC= () => {
    return (
        <div>
            <CommonButton btnLevel="Book Appointment" onClick={()=>{}}/>
        </div>
    );
};

export default AppointmentButton;